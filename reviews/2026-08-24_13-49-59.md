# Brutal Critique Panel Review

**Date**: 2026-08-24 13:49:59
**Branch**: opencode-test
**Commit**: b6f3ea1
**Files Inspected**: 35

**Runtime Evidence**: No `/dev/ttyUSB*` devices present. Port 5000 free. No ROS 2 nodes running. `colcon test --packages-select hoverboard_control`: 2 failures (flake8, pep257), 1 skipped (copyright). Pre-existing dirty baseline: deleted `src/docs/*` files, untracked `.github/`, `rclpy`, `threading`.

---

## Dr. Viktor — Embedded & Hardware

#### Finding 1 — Might Cause Problems (P0) — `EspCode/espcode/head_esp_code.ino:82-93,124-135`
> "Adham, your head motor has no timeout. One `Y` byte and it spins forever. The Hold button sends nothing — air doesn't stop a stepper. Your firmware only brakes on lowercase `y/z/u/d`, but the UI doesn't send those. You're one loose finger away from a burnt motor or a smashed face."
- Evidence: `EspCode/espcode/head_esp_code.ino:82-93,124-135`
- Simple plan: 1) Add `lastCmdMs` watchdog in `loop()` — if `millis()-lastCmdMs > 300` call `stopRotation()` + `dcBrake()` 2) Wire Hold in `manual_web_controller.py:51` to send `y` + `u` 3) Bench test: press Y, wait 1s, confirm stopped

#### Finding 2 — Might Cause Problems (P0) — `EspCode/espcode/hoverboard_code.ino:186-203`
> "Adham, your ultrasonic code is fiction. It never waits for the ECHO pin to go HIGH, then divides an already-centimetre number by 1000. Rear distance reads zero always. Reverse has no rear interlock. You'll back into a wall at full speed."
- Evidence: `EspCode/espcode/hoverboard_code.ino:186-203`
- Simple plan: 1) Rewrite HC-SR04 with rising-edge → falling-edge state machine 2) Compute `distance_cm = duration_us * 0.0343f / 2.0f` once, no `/1000` 3) Publish `U:invalid` on timeout; fail-closed on reverse in `manual_controller.py`

#### Finding 3 — Better to Change (P1) — `EspCode/espcode/head_esp_code.ino:86-92`; `manual_controller.py:117-120`; `docs/aiSummary.md:14-18`
> "Adham, three different protocols in one robot. Firmware says lowercase = stop. Python says lowercase = move. Docs say lowercase = uppercase and silence = stop. Pick one. Encode it as constants. Reject anything else."
- Evidence: `EspCode/espcode/head_esp_code.ino:86-92`; `manual_controller.py:117-120`; `docs/aiSummary.md:14-18`
- Simple plan: 1) Create `PROTOCOL.md` with single contract 2) Add `HEAD_LEFT='Y', HEAD_LEFT_STOP='y'` constants in firmware + ROS 3) Update all three places to match

#### Finding 4 — Better to Change (P1) — `EspCode/espcode/head_esp_code.ino:21-25`
> "Adham, GPIO2 is a boot strap pin. You tied a motor PWM to it without documenting the pull-up/down or driver-enable state. Cold boot could lock the ESP8266 or twitch the head. Move PWM off GPIO2 or document the hardware fix."
- Evidence: `EspCode/espcode/head_esp_code.ino:21-25`
- Simple plan: 1) Move PWM/enable to a safe pin (GPIO4, GPIO5, GPIO14) 2) Document external pull-up/down and driver-enable logic 3) Test cold boot on bench

---

## Elena — System Architecture

#### Finding 1 — Might Cause Problems (P0) — `hoverboard_control/head_controller.py:22,109-144`
> "Adham, `head_controller.py` hardcodes `/dev/ttyUSB2`. That device doesn't exist — your head is on `/dev/ttyUSB1`. It also assumes silence stops the motor. Your firmware has no silence timeout. Face tracking is dead code, and if you ever fix the port, face-loss won't brake."
- Evidence: `hoverboard_control/head_controller.py:22,109-144`; `EspCode/espcode/head_esp_code.ino:82-93`
- Simple plan: 1) Delete `head_controller.py` and its `setup.py` entry 2) If you need face tracking, rebuild it on a shared head driver with real stop semantics 3) Remove dead entry point

#### Finding 2 — Better to Change (P1) — `hoverboard_control/manual_controller.py:175-176,222-233`
> "Adham, you catch `Exception` and log nothing. Then you claim to rate-limit with `int(now.nanoseconds/1e9)%1==0` — that's always true, so you spam 10 Hz. Serial drops and parse errors vanish. During a crash you'll have zero forensic trail."
- Evidence: `hoverboard_control/manual_controller.py:175-176,222-233`
- Simple plan: 1) Catch `serial.SerialException` and `UnicodeDecodeError` separately 2) Track health timestamps, reconnect with backoff 3) Replace bogus rate-limit with `time.monotonic() - last_log > 1.0`

#### Finding 3 — Better to Change (P1) — `hoverboard_control/manual_controller.py:20-21`; `hoverboard_control/fusion_controller.py:69-79`
> "Adham, two drivers fighting for one serial port. Manual and autonomous stacks each open the base independently with different discovery logic. Launch separation is just a comment. Start both and you get a serial ownership race and conflicting motor commands."
- Evidence: `hoverboard_control/manual_controller.py:20-21`; `hoverboard_control/fusion_controller.py:69-79`
- Simple plan: 1) Create one serial-driver node with lock/lease 2) Parameterize stable device paths via `/dev/serial/by-id/*` 3) Add `mode:=manual|auto` launch arg that prevents dual-stack start

#### Finding 4 — Better to Change (P1) — `hoverboard_control/manual_controller.py:49-50,197-203`
> "Adham, depth value 0 and rear telemetry defaults are treated as 'no obstacle'. A failed camera or dead ultrasonic removes the safety interlock silently. No fault state, no warning, no operator acknowledgement. The robot just keeps driving."
- Evidence: `hoverboard_control/manual_controller.py:49-50,197-203`
- Simple plan: 1) Track sensor freshness + validity 2) Publish `/robot/state` fault enum on stale/invalid 3) Fail-closed: block forward if front invalid, block reverse if rear invalid

---

## Kai — UI & Frontend

#### Finding 1 — Might Cause Problems (P0) — `hoverboard_control/manual_web_controller.py:51-53,77`
> "Adham, your Hold button is a lie. UI says 'silence = stop'. It sends no request. Firmware needs lowercase `y/z/u/d` to brake. You press Hold, the head keeps spinning. That's not a UI bug, that's a safety hazard."
- Evidence: `hoverboard_control/manual_web_controller.py:51-53,77`; `EspCode/espcode/head_esp_code.ino:82-93`
- Simple plan: 1) Add explicit stop endpoints `/head/stop_pan` + `/head/stop_tilt` 2) Wire Hold to `fetch('/head/stop_pan')` + `fetch('/head/stop_tilt')` 3) Update UI copy only after firmware watchdog is live

#### Finding 2 — Better to Change (P1) — `hoverboard_control/manual_web_controller.py:61-62,101-107`
> "Adham, base and emotion buttons update status *before* checking the HTTP response. Flask returns `{'ok': true}` even when the ROS node is down or the command is rejected. The operator sees green but nothing happened."
- Evidence: `hoverboard_control/manual_web_controller.py:61-62,101-107`
- Simple plan: 1) Return 503 when `node is None`, 400 on invalid cmd 2) Only update status on `response.ok` 3) Add persistent offline banner + disabled controls

#### Finding 3 — Better to Change (P1) — `hoverboard_control/web_controller.py:64-92`
> "Adham, the autonomous UI silently swallows every `fetch` failure — movement, pause, emotions, startup state, mode polling. Network down? Port conflict? Server error? The UI still looks green. Operator has zero visibility."
- Evidence: `hoverboard_control/web_controller.py:64-92`
- Simple plan: 1) Centralize `fetch` with timeout + retry + error state 2) Show persistent offline banner 3) Disable controls on error

#### Finding 4 — Need to Change (P2) — `hoverboard_control/manual_web_controller.py:13-81`
> "Adham, HTML/CSS/JS stuffed in a Python triple-quoted string. No browser test coverage, no cache versioning. Browser cache serves stale controllers. Changing the UI means editing a 700-line string. Hostile to maintenance."
- Evidence: `hoverboard_control/manual_web_controller.py:13-81`
- Simple plan: 1) Extract assets to `resource/web/` as versioned static files 2) Add lightweight Playwright/pytest-flask API test 3) Serve `Cache-Control: no-cache` in dev

---

## Marcus — DevOps & Operations

#### Finding 1 — Might Cause Problems (P0) — `test/test_flake8.py:21-25`; `test/test_pep257.py:21-23`
> "Adham, `colcon test` fails on flake8 and pep257. Only copyright test passes. Your quality gate is permanently red — it blocks nothing and lies about test coverage. You have zero behavioral tests for serial framing, safety overrides, or stop behavior."
- Evidence: `test/test_flake8.py:21-25`; `test/test_pep257.py:21-23`
- Simple plan: 1) Run `black` + `autopep8` until lint passes 2) Add 3 behavioral tests: base serial framing, front depth override, head stop on Hold 3) Remove or fix the style gates

#### Finding 2 — Better to Change (P1) — `package.xml:6-12`; `hoverboard_control/manual_controller.py:10-16`; `hoverboard_control/eilik_bridge.py:4`
> "Adham, `package.xml` has TODO metadata and misses `geometry_msgs`, `cv_bridge`, `pyserial`, `numpy`, `flask`, `requests`, `opencv`, `mediapipe`. A clean machine can't reproduce runtime from package metadata. Deployment relies on ambient installs."
- Evidence: `package.xml:6-12`; `hoverboard_control/manual_controller.py:10-16`; `hoverboard_control/eilik_bridge.py:4`
- Simple plan: 1) Declare all ROS deps in `package.xml` 2) Add Python deps via `rosdep` + `setup.py` `install_requires` 3) Pin to Jazzy (your runtime), not Humble

#### Finding 3 — Better to Change (P1) — `docs/DEPENDENCIES.md:9-11,37-43`
> "Adham, your dependency guide mandates Ubuntu 22.04, ROS Humble, Python 3.10. You're running ROS Jazzy. Setup instructions point to incompatible packages. Pick one supported matrix and test it."
- Evidence: `docs/DEPENDENCIES.md:9-11,37-43`
- Simple plan: 1) Update `DEPENDENCIES.md` to Jazzy + Ubuntu 24.04 2) Remove Humble-only install commands 3) Add CI to test the declared matrix

#### Finding 4 — Better to Change (P1) — `docs/HARDWARE.md:1-7`; `docs/SOFTWARE.md:32-50`
> "Adham, `HARDWARE.md` is wrapped in an unclosed markdown fence. `SOFTWARE.md` names deleted `espcode/espcode.ino` and claims a head timeout your firmware doesn't have. Hardware setup and safety behavior are undocumented exactly where a builder needs precision."
- Evidence: `docs/HARDWARE.md:1-7`; `docs/SOFTWARE.md:32-50`; `EspCode/espcode/`
- Simple plan: 1) Rewrite `HARDWARE.md` as valid markdown 2) List `head_esp_code.ino` + `hoverboard_code.ino` 3) Generate protocol tables from single `PROTOCOL.md` contract

#### Finding 5 — Better to Change (P1) — `launch/WebCamera.launch.py:10-15,70-76`; `launch/camera.launch.py:7-8,56-62`
> "Adham, launch files hardcode hotspot name, source-tree Electron path, differing `/dev/video*` enumeration, and port-5000 controllers without stable device config. Deployment breaks when hardware enumeration changes. Two launch files are mutually destructive but not mechanically prevented."
- Evidence: `launch/WebCamera.launch.py:10-15,70-76`; `launch/camera.launch.py:7-8,56-62`
- Simple plan: 1) Add `LaunchConfiguration` args for `head_port`, `base_port`, `video_device`, `hotspot_name` 2) Use `get_package_share_path` for Electron 3) Use `/dev/serial/by-id/*` + udev rules 4) Block incompatible mode starts via single `mode` arg

---

## Cross-Examination — Judges Talking To Each Other

Viktor: "Elena, your ROS code thinks silence stops the head motor. It doesn't. I wrote the firmware — it only brakes on lowercase bytes. The UI sends nothing on Hold."

Elena: "And Viktor, even if you add a watchdog, Kai's button still sends air. The human presses Hold and the motor laughs. We need explicit stop endpoints."

Kai: "Hey, I just draw buttons. Viktor gives me air to work with, Elena gives me a ROS node that lies about success. What do you want me to do?"

Marcus: "And I watch all of you claim 'it works' while `colcon test` has been red since day one. Cute. Your docs say Humble, your runtime is Jazzy, your hardware doc is broken markdown. Nobody verified a clean install."

---

## Scorecard

| Judge | Domain | Score | Says |
|-------|--------|-------|------|
| Dr. Viktor | Embedded & Hardware | 2.0 / 10.0 | "2/10. Your head can run forever. I don't give points for hope." |
| Elena | System Architecture | 3.0 / 10.0 | "3/10. Two drivers, one port — that's not architecture." |
| Kai | UI & Frontend | 4.0 / 10.0 | "4/10. Your Hold button is decoration." |
| Marcus | DevOps & Operations | 2.5 / 10.0 | "2.5/10. Red tests block nothing. So why have them?" |
| **Average** | **Composite** | **2.9 / 10.0** | |

---

## Remediation Roadmap

### Might Cause Problems (P0) — Fix these or something will break
**Viktor says:** "Adham, first, stop the head from spinning forever. 30 minutes now saves a burnt motor later. Do this before you let anyone touch the robot."
- [ ] Add watchdog in `head_esp_code.ino:82` — call `stopRotation()` + `dcBrake()` after 300ms silence
- [ ] Wire Hold to send `y` + `u` in `manual_web_controller.py:51`
- [ ] Bench test: press Y, wait 1 sec, confirm stopped

**Viktor says:** "Second, fix the ultrasonic math. It's not 'wrong', it's fiction. Rear interlock is a lie."
- [ ] Rewrite HC-SR04 state machine in `hoverboard_code.ino:186`
- [ ] Publish `U:invalid` on timeout; fail-closed reverse in `manual_controller.py`
- [ ] Test: 10cm obstacle → ~10cm, no echo → invalid, reverse blocked

**Elena says:** "Third, kill `head_controller.py`. It's dead code on the wrong port with wrong stop semantics."
- [ ] Delete `head_controller.py` + `setup.py` entry
- [ ] If face tracking needed, rebuild on shared head driver with real stops

**Marcus says:** "Fourth, make the test gate green. A red lint-only suite is theater, not quality."
- [ ] Run `black` + `autopep8` until flake8/pep257 pass
- [ ] Add 3 behavioral tests: base framing, depth override, head stop
- [ ] Verify `colcon test` = 0 failures

### Better to Change (P1) — Will bite you later
**Elena says:** "Write one `PROTOCOL.md`. Base: `F/B/L/R/S\n` @115200 to ESP32. Head: `Y/Z/U/D` + `y/z/u/d` brake + 300ms watchdog. Share constants. Reject unknown bytes."
- [ ] Create `docs/PROTOCOL.md` versioned contract
- [ ] Sync firmware + ROS + UI + docs to it

**Elena says:** "One serial driver, parameterized device paths, mutual-exclusion launch mode. No more dual-stack races."
- [ ] Create serial-driver node with lock/lease
- [ ] Add `mode:=manual|auto` arg that prevents dual-stack start

**Elena says:** "Sensor failure = fault state, not 'no obstacle'. Track freshness, fail closed."
- [ ] Publish `/robot/state` fault on stale/invalid depth or ultrasonic
- [ ] Block forward if front invalid, block reverse if rear invalid

**Kai says:** "Stop lying to the operator. 503 on node down, 400 on bad cmd. Offline banner. Disabled controls."
- [ ] Centralize `fetch` with timeout/retry/error state
- [ ] Return honest HTTP codes

**Marcus says:** "Fix `package.xml` + `DEPENDENCIES.md`. Declare Jazzy + Ubuntu 24.04. Pin all deps."
- [ ] Add missing ROS + Python deps to `package.xml`
- [ ] Update `DEPENDENCIES.md` to match reality

**Marcus says:** "Rewrite hardware/software docs. Valid markdown. Actual firmware files. Generated protocol tables."
- [ ] Fix `HARDWARE.md` fence + list `head_esp_code.ino`/`hoverboard_code.ino`
- [ ] Fix `SOFTWARE.md` timeout claims
- [ ] Generate tables from `PROTOCOL.md`

### Need to Change (P2) — Polish when you have time
**Kai says:** "Extract the inline web assets. Static files. Versioned. Tested. Cache headers."
- [ ] Move HTML/CSS/JS to `resource/web/`
- [ ] Add browser/API test
- [ ] Dev `Cache-Control: no-cache`

**Marcus says:** "Clean up the pre-existing garbage. `src/docs/*` deletions, `.github/`, `rclpy/`, `threading/`. Decide: commit or delete."
- [ ] Resolve untracked/pre-existing state

---

### Your To-Do List (copy this)
- [ ] Head watchdog + Hold wiring (P0) — `head_esp_code.ino:82`, `manual_web_controller.py:51`
- [ ] Fix ultrasonic math (P0) — `hoverboard_code.ino:186`
- [ ] Delete/fix `head_controller.py` (P0) — `head_controller.py:22`
- [ ] Make colcon test green (P0) — `black` + behavioral tests
- [ ] Write `PROTOCOL.md` single contract (P1)
- [ ] Consolidate serial driver + launch mutual exclusion (P1)
- [ ] Fault states for sensor freshness (P1)
- [ ] Truthful Flask responses + offline banner (P1)
- [ ] Fix `package.xml` + `DEPENDENCIES.md` for Jazzy (P1)
- [ ] Rewrite `HARDWARE.md`/`SOFTWARE.md` from `PROTOCOL.md` (P1)
- [ ] Extract web assets to static files (P2)
- [ ] Clean pre-existing dirty baseline (P2)